export class AutorModel {
    id: string;
    Nome: string;
    
    constructor() {}
    
    /*postagens: PostModel[];

    addItem(item: PostModel){
        this.postagens.push({
            titulo: item.titulo,
            autor: item.autor,
            postagem: item.postagem
        });
    }
 
    removeItem(item){
        for(let i = 0; i < this.postagens.length; i++) {
            if(this.postagens[i] == item){
                this.postagens.splice(i, 1);
            }
        }
 
    }*/
}